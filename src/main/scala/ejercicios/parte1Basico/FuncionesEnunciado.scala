package ejercicios.parte1Basico

import scala.annotation.tailrec

/**
 * @see Soluciones disponibles en [[FuncionesSolucion]]
 */
object FuncionesEnunciado {

  def main(args: Array[String]): Unit = {
    //Ejecuta aquí
  }

  /**
   * Función que recibe por parámetro un nombre y una edad y generar un string con un mensaje de saludo.
   *
   * @param nombre nombre de la persona que está saludando.
   * @param edad   edad de la persona que está saludando.
   * @return Mensaje de saludo dirigido a otra persona.
   */
  def saludoParaNiños(nombre: String, edad: Int): String = {
    ???
  }

  /**
   * Función que calcula el valor factorial de un número
   *
   * @param número número sobre el que calcular su valor factorial.
   * @return el valor del factorial del número.
   */
  def factorial(número: Int): Int = {
    ???
  }

  /**
   * Función que calcula el valor Fibonacci de un número.
   *
   * @param número número sobre el que calcular Fibonacci.
   * @return el valor de Fibonacci del número.
   */
  def fibonacci(número: Int): Int = {
    ???
  }

  /**
   * Función que evalúa si un número es primo (true) o no (false)
   *
   * @param número número a evaluar
   * @return true si el número es primo, false si no lo es.
   */
  def esPrimo(número: Int): Boolean = {
    ???
  }
}
